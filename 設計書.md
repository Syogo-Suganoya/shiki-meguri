# シキめぐり 設計書

冠婚葬祭レンタル×移動エージェント（CANDIDATE 11）

## 1. 概要

結婚式・成人式・観光着物・葬儀の「式」の衣装を、バーチャル試着で選定し、受取場所の最適化・当日の逆算動線・返却管理までエージェントが一気通貫で仕切るサービス。

- **一言価値**: 衣装レンタルの「借りるまで」しかなかった体験を「式が終わって返すまで」に拡張する
- **テーマ適合**: 金銭・手配・慶弔情報という繊細な領域で、自律実行と本人同意・データ最小化を両立する設計を見せる

## 2. 課題

| 課題 | 現状 |
|---|---|
| 試着のための移動 | 店頭試着が前提。宅配レンタルは「似合うか」が届くまで不明 |
| 衣装と移動の分断 | レンタル各社は配送まで。当日の動線（大荷物・着てから移動）は本人任せ |
| 返却の綱渡り | 返却期限と式後の疲れ・二次会・遠方帰路が衝突。延滞金トラブルが慢性化 |

既存: Cariru等（試着・宅配）、喪服レスキュー等（即日）はあるが「会場受取×当日動線×返却管理」の統合は存在しない（2026-08 検索確認済み）。

## 3. ユースケース

1. **結婚式お呼ばれ**: パーソナルカラー→ドレス試着→会場最寄り受取→式後コンビニ発送
2. **成人式・卒業式**: 振袖・袴の試着→着付け枠と式典の逆算タイムライン
3. **観光着物**（旧キモノめぐり）: 試着予約→着物制約の観光動線→返却時刻の逆算監視
4. **葬儀**: 訃報からの残り時間で喪服一式を最短手配→斎場最寄り受取

## 4. システムアーキテクチャ

```
[Web PWA（自前チャットUI）]
      │
      ▼
[API Gateway (Cloud Run: api)]
      │
      ▼
[Orchestrator Agent (ADK / Cloud Run: agent)]
  ├── 試着エージェント ──────── YouCam API (Clothes/Fabric Try-On, Facial Color Tones)
  ├── 手配エージェント ──────── レンタル事業者API（モック）/ 受取地最適化
  ├── 動線エージェント ──────── 駅すぱあと API MCPサーバー
  └── 返却監視エージェント ──── Cloud Scheduler（期限・進捗ポーリング）
      │
      ▼
[Firestore]（予約・動線・返却状態・会話履歴 / TTL削除）※開発はエミュレータ
[Cloud Logging]（構造化監査ログ：判断根拠・同意記録・画像破棄証跡）
```

### エージェント構成（ADK）

| エージェント | 役割 | 自律性 |
|---|---|---|
| Orchestrator | 会話の受付、シーン判定（慶/弔）、子エージェントへの委譲 | ユーザー対話駆動 |
| 試着 | 候補絞り込み（パーソナルカラー×ドレスコード）、試着画像生成 | 提案まで |
| 手配 | 受取場所の比較（自宅配送/店舗/ロッカー）、予約起案 | **金銭確定は本人同意必須** |
| 動線 | 逆算タイムライン生成、当日の遅延監視・再計算 | 再計算・通知は自律実行 |
| 返却監視 | 式後の予定と期限の突合、返却手順提案、延長起案 | 提案まで |

## 5. 使用API・技術スタック

| 区分 | 技術 |
|---|---|
| 実行基盤（必須要件） | Cloud Run（api / agent / worker）、Cloud Scheduler |
| AI（必須要件） | Gemini API（判断・提案文・慶弔マナー考慮）、Agent Development Kit |
| スポンサー | YouCam API（AI Clothes Try-On, Facial Color Tones Analyzer）、駅すぱあと API MCPサーバー |
| データ | Firestore（TTLポリシー付き。開発はエミュレータ、保存先はこれ一本） |
| 通知 | 自前チャット（Firestore `messages` / 外部メッセージング非依存）。通知はアプリ内で完結し、端末へのプッシュは対象外 |

## 6. データモデル（Firestore）

```
users/{uid}
  profile: { 最寄り駅, サイズ, パーソナルカラー結果(スコアのみ) }
events/{eventId}
  type: wedding | seijin | tourism | funeral
  schedule: { 式日時, 会場, 受取, 返却期限 }
  outfit: { 選定衣装, レンタル状態 }
  route: { 当日タイムライン, 再計算履歴 }
  ttl: 式終了 + 7日で自動削除
audit/{logId}   # 追記専用
  { ts, agent, action, basis, consent_ref, image_destroyed_at }
```

## 7. ガバナンス&セキュリティ設計

1. **試着画像の即時破棄**: 顔・全身画像は試着結果生成後に削除し、破棄時刻を監査ログへ記録。弔事は保持期間をさらに短縮
2. **慶弔情報の最小化**: 「誰の式か」「誰が亡くなったか」は収集しない。日時・会場・服装区分のみで全機能が成立
3. **金銭・手配の同意ゲート**: レンタル確定・延長料金・発送はエージェント権限外。起案→本人承認→実行を監査ログに残す
4. **可観測性**: 全エージェント判断を `audit` コレクションに構造化記録。デモで監査ビューを見せる

## 8. 環境構築（Docker）

ローカル開発から本番まで、すべてコンテナで統一する（Cloud Run はコンテナデプロイのため、ローカルと本番で同一イメージを使用できる）。

### 構成

```
.
├── docker-compose.yml        # ローカル開発一式
├── services/
│   ├── api/Dockerfile        # API Gateway（FastAPI）
│   ├── agent/Dockerfile      # ADKエージェント本体
│   └── web/Dockerfile        # フロント（PWA、開発時のみ）
└── .env.example              # APIキーのテンプレート（実キーはコミットしない）
```

### docker-compose サービス

| サービス | 内容 |
|---|---|
| api | API Gateway。ホットリロード有効（ソースをボリュームマウント） |
| agent | ADKエージェント。Gemini API・スポンサーAPIへの接続を担う |
| web | フロント開発サーバー |
| firestore-emulator | Firestoreエミュレータ（gcloud CLIイメージ）。本番データに触れず開発 |

- `docker compose up` で全サービス起動。APIキー類は `.env`（gitignore対象）から注入
- 本番の秘密情報は Secret Manager で管理し、Cloud Run に環境変数として注入（イメージに焼き込まない）

### イメージとデプロイ

- ベースイメージ: `python:3.12-slim`（マルチステージビルドで依存を分離し軽量化）
- デプロイ: `gcloud builds submit` → Cloud Build でビルド → Artifact Registry へ push → Cloud Run へリリース
- ローカル・CI・本番が同一 Dockerfile を参照し、環境差異による不具合を構造的に排除

## 9. MVPスコープ（〜10/15）

| 週 | マイルストーン |
|---|---|
| W1 | ADK雛形＋駅すぱあとMCP接続、Firestoreスキーマ |
| W2 | YouCam試着パイプライン（結婚式ユースケースのみ） |
| W3 | 動線エージェント（逆算タイムライン・遅延再計算） |
| W4 | 同意ゲート・監査ログ・返却監視 |
| W5 | チャットUI・デモデータ整備・デプロイ（テストアカウント） |
| W6 | デモ動画・アーキテクチャ図・提出物 |

**MVPで捨てるもの**: 葬儀・成人式ユースケース（LP上のビジョンとして提示）、実レンタル事業者連携（モックAPI）

## 10. 審査基準との対応

| 基準 | 対応 |
|---|---|
| 課題の新規性 | 「試着〜返却」統合の空白（検索で立証済み）。慶弔両対応は独自 |
| 自律性・エージェントらしさ | 遅延監視→再計算→リスケ提案の自律ループ＋同意ゲートのHITL |
| 実装品質と拡張性 | 4ユースケースが同一コンポーネント（試着/動線/返却）の構成差で成立 |
